# Staging Environment Overrides
# This file overrides values from .env.dev

# Disable debug mode in staging
DEBUG=false

# Disable dev auth bypass in staging
DEV_AUTH_BYPASS=false

# Use production-like settings
# Add other staging-specific overrides here as needed